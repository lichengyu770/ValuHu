# ValuHub 算法模块

**版本**: v1.0  
**创建日期**: 2025年12月31日  
**负责人**: 「大脑」AI  
**模块类型**: AI估价算法与数据分析

---

## 📋 目录

1. [模块概述](#模块概述)
2. [核心算法](#核心算法)
3. [数据清洗](#数据清洗)
4. [市场分析](#市场分析)
5. [使用指南](#使用指南)
6. [性能优化](#性能优化)

---

## 模块概述

### 功能描述
ValuHub算法模块提供完整的房产价值评估和数据分析能力，包括：
- AI估价模型训练和预测
- 数据清洗和预处理
- 市场分析图表生成
- 模拟数据生成

### 技术栈
- **机器学习**: Scikit-learn, TensorFlow
- **数据处理**: Pandas, NumPy
- **可视化**: Matplotlib
- **模型存储**: Joblib

---

## 核心算法

### ValuationModel类
房产估价模型，支持多种机器学习算法。

#### 支持的算法
1. **线性回归 (Linear Regression)**
   - 简单快速
   - 可解释性强
   - 适合小数据集

2. **随机森林 (Random Forest)**
   - 准确度高
   - 处理非线性关系
   - 特征重要性分析

3. **集成模型 (Ensemble)**
   - 结合多个模型
   - 提高预测稳定性
   - 降低过拟合风险

#### 核心方法
- `prepare_features(df)` - 特征工程
- `train(X, y)` - 训练模型
- `predict(X)` - 预测价格
- `evaluate(X, y)` - 评估模型性能
- `save_model(filepath)` - 保存模型
- `load_model(filepath)` - 加载模型

#### 特征工程
- **数值特征标准化**: 使用StandardScaler
- **分类特征编码**: 使用LabelEncoder
- **衍生特征生成**:
  - 每房间面积 (area_per_room)
  - 房产年龄 (property_age)
  - 年龄平方 (age_squared)
  - 楼层面积比 (floor_area_ratio)

#### 模型评估指标
- **均方误差 (MSE)**: 预测误差的平方平均值
- **均方根误差 (RMSE)**: MSE的平方根
- **R²分数**: 模型解释的方差比例
- **平均绝对百分比误差 (MAPE)**: 预测误差的百分比

---

## 数据清洗

### DataCleaner类
数据清洗类，提供完整的数据清洗流程。

#### 清洗方法
1. **移除重复记录**
   - 基于所有列检测重复
   - 保留第一条记录
   - 记录移除数量

2. **处理缺失值**
   - 策略: median, mean, mode, drop
   - 数值列使用中位数/平均值填充
   - 分类列使用众数填充
   - 记录填充日志

3. **移除异常值**
   - IQR方法: 基于四分位数
   - Z-score方法: 基于标准差
   - 百分位数方法: 基于分位数
   - 记录异常值数量

4. **标准化文本数据**
   - 移除前后空格
   - 统一大小写
   - 移除特殊字符

5. **验证数据范围**
   - 面积: 30-500 平方米
   - 楼层: 1-50 层
   - 建筑年份: 1990-2024
   - 房间数: 1-10 间
   - 卫生间数: 1-5 间
   - 价格: 10万-1000万

#### 清洗报告
- 初始记录数
- 最终记录数
- 清洗日志
- 数据形状
- 列名
- 缺失值统计
- 数据类型

---

## 市场分析

### MarketAnalyzer类
市场分析器，生成各种可视化图表。

#### 图表类型
1. **价格趋势图**
   - 按月份聚合数据
   - 双Y轴：价格趋势 + 成交量
   - 支持城市和区域筛选

2. **区域对比图**
   - 多城市平均价格对比
   - 多城市每平米价格对比
   - 柱状图 + 数值标签

3. **价格分布图**
   - 价格直方图 + 统计信息
   - 各类型房产价格箱线图
   - 平均值和中位数标注

4. **房产类型饼图**
   - 各类型房产占比
   - 百分比标注
   - 颜色区分

#### 图表配置
- **中文字体**: SimHei, Arial Unicode MS
- **DPI**: 300 (高清晰度)
- **颜色方案**: ValuHub品牌色系
- **输出格式**: PNG

---

## 使用指南

### 快速开始

#### 1. 生成模拟数据
```bash
cd algorithms
python valuation_model.py
```

#### 2. 训练估价模型
```bash
cd algorithms
python valuation_model.py
```

#### 3. 清洗数据
```bash
cd algorithms
python data_cleaner.py
```

#### 4. 生成市场分析图表
```bash
cd algorithms
python market_analyzer.py
```

### Python API使用

#### 训练模型
```python
from algorithms import train_valuation_model

model = train_valuation_model(
    data_path='sample_data.csv',
    model_type='random_forest',
    test_size=0.2,
    output_path='valuation_model.pkl'
)
```

#### 预测价格
```python
from algorithms import predict_price

property_features = {
    'area': 100,
    'floor_level': 15,
    'building_year': 2015,
    'rooms': 3,
    'bathrooms': 2,
    'property_type': 'residential',
    'orientation': '南',
    'decoration_status': '精装',
    'city': '北京',
    'district': '朝阳区'
}

result = predict_price(
    model_path='valuation_model.pkl',
    property_features=property_features
)

print(f"预测价格: {result['predicted_price']}")
print(f"每平米价格: {result['price_per_sqm']}")
print(f"置信度: {result['confidence']}")
```

#### 清洗数据
```python
from algorithms import clean_property_data

cleaned_df = clean_property_data(
    input_path='raw_data.csv',
    output_path='cleaned_data.csv'
)
```

#### 生成分析图表
```python
from algorithms import generate_market_analysis_report

charts = generate_market_analysis_report(
    data_path='cleaned_data.csv',
    output_dir='charts'
)
```

---

## 性能优化

### 模型性能
- **训练时间**: < 5分钟 (5000条数据)
- **预测时间**: < 10ms (单条记录)
- **内存占用**: < 100MB
- **准确率**: > 85% (R²分数)

### 数据处理性能
- **清洗时间**: < 1分钟 (10000条数据)
- **图表生成时间**: < 30秒 (10个城市)
- **文件I/O**: 优化批量读写

### 优化建议
1. **使用增量训练**: 新数据到达时增量更新模型
2. **模型缓存**: 缓存训练好的模型，避免重复加载
3. **并行处理**: 使用多进程处理大数据集
4. **特征选择**: 选择最重要的特征，减少计算量

---

## 依赖安装

```bash
pip install scikit-learn pandas numpy matplotlib joblib
```

---

## 项目结构

```
algorithms/
├── __init__.py              # 模块初始化
├── valuation_model.py        # 估价模型
├── data_cleaner.py          # 数据清洗
└── market_analyzer.py        # 市场分析
```

---

## 下一步

1. 集成到后端API
2. 实现实时模型更新
3. 添加更多特征
4. 优化模型性能
5. 添加深度学习模型

---

**文档状态**: ✅ 已完成  
**下一步**: 2.4 持续测试（「卫士」AI）
