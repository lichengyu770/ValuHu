import React from 'react';
import BasePage from '../BasePage';

const Plugins = () => {
  return (
    <BasePage
      title='插件市场页'
      description='展示第三方适配插件（如GIS地图、房产数据对接），支持在线安装。'
    />
  );
};

export default Plugins;
