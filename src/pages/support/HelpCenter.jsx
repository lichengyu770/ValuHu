import React from 'react';
import BasePage from '../BasePage';

const HelpCenter = () => {
  return (
    <BasePage
      title='帮助中心'
      description='按问题分类（注册/接口/计费）展示FAQ，支持关键词搜索。'
    />
  );
};

export default HelpCenter;
