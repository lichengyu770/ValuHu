import React from 'react';
import BasePage from '../BasePage';

const Api = () => {
  return (
    <BasePage
      title='API接口页'
      description='提供接口文档、调用示例、SDK下载，支持在线调试接口。'
    />
  );
};

export default Api;
