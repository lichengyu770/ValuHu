import React from 'react';
import BasePage from '../BasePage';

const Docs = () => {
  return (
    <BasePage
      title='开发文档页'
      description='含接口说明、错误码、签名规则，支持在线搜索与版本切换。'
    />
  );
};

export default Docs;
